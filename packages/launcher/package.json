{
  "name": "@konteks/remote-launcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "konteks-remote: native BYOA agent connector installation, user service lifecycle, status, and official agent authentication.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "bin": {
    "konteks-remote": "./dist/cli.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --build --noEmit",
    "test": "vitest run --root ../.. --project unit -- packages/launcher"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@konteks/remote-common": "*",
    "@konteks/remote-release": "*",
    "@konteks/remote-supervisor": "*",
    "commander": "^13.0.0",
    "ws": "^8.18.0",
    "zod": "^4.0.0"
  }
}
